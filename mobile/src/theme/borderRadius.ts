/**
 * 无障碍出行陪伴平台 — 圆角 Design Token
 *
 * 严格对照 UI 设计规范第五章定义（6 级圆角系统）。
 *
 * | Token     | 值     | 使用场景                             |
 * |-----------|--------|--------------------------------------|
 * | sm        | 8px    | 导航按钮、环境警告右侧圆角            |
 * | md        | 12px   | 表单输入框、列表项图标、障碍警告       |
 * | lg        | 16px   | 卡片、导航栏、地图区域、模态框         |
 * | xl        | 20px   | Hero 底部、模态框                     |
 * | full      | 9999px | 胶囊按钮、搜索框、标签、头像、进度条   |
 */

export const borderRadius = {
  /** 8px — 导航按钮、环境警告右侧圆角 */
  sm: 8,
  /** 12px — 表单输入框、列表项图标、障碍警告 */
  md: 12,
  /** 16px — 卡片、导航栏、地图区域、模态框 */
  lg: 16,
  /** 20px — Hero 底部、模态框 */
  xl: 20,
  /** 9999px — 胶囊按钮、搜索框、标签、头像、进度条 */
  full: 9999,
} as const;

export type BorderRadiusTokens = typeof borderRadius;
