/**
 * 无障碍出行陪伴平台 — 间距 Design Token
 *
 * 严格对照 UI 设计规范第四章定义（8 级间距系统）。
 *
 * | Token    | 值   | 使用场景                         |
 * |----------|------|----------------------------------|
 * | xs       | 4px  | 图标与文字间隙、标签内间距        |
 * | sm       | 8px  | 标签栏内边距、标签间距、卡片小间距 |
 * | md       | 12px | 卡片内边距、头像与文字、列表项间距 |
 * | lg       | 16px | 卡片和页面主要内边距、表单间距     |
 * | xl       | 20px | Hero 区、区块间距                 |
 * | 2xl      | 24px | Hero 顶部内边距                   |
 * | 3xl      | 32px | Hero 底部内边距、个人中心头        |
 */

export const spacing = {
  /** 4px — 图标与文字间隙、标签内间距 */
  xs: 4,
  /** 8px — 标签栏内边距、标签间距、卡片内小间距 */
  sm: 8,
  /** 12px — 卡片内边距、头像与文字、列表项间距 */
  md: 12,
  /** 16px — 卡片和页面主要内边距、表单间距 */
  lg: 16,
  /** 20px — Hero 区、区块间距 */
  xl: 20,
  /** 24px — Hero 顶部内边距 */
  '2xl': 24,
  /** 32px — Hero 底部内边距、个人中心头部 */
  '3xl': 32,
} as const;

export type SpacingTokens = typeof spacing;
